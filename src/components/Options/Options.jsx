import Button from "../Button/Button";

const Options = () => {
  return (
    <>
      <Button>Good</Button>
      <Button>Neutral</Button>
      <Button>Bad</Button>
      <Button>Reset</Button>
    </>
  );
};

export default Options;
